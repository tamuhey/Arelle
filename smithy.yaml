project: arelle
language: arelle
runner_image: docker.webfilings.org/devops/smithy-runner:python3.4

env:
  - PATH=/tmp/virtualenv/bin:$PATH
  - PYTHONPATH=.

script:
  - ./smithy_arelle.sh

after_script:
  -

artifacts:
  pypi:
    - /dist/arelle*.tar.gz